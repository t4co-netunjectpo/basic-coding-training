<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="TodoApp_WinUI.MainPage"
    x:Name="Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:TodoApp_WinUI"
    xmlns:todos="using:TodoApp.Application.Todos"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer HorizontalScrollBarVisibility="Disabled">
        <Grid Padding="32"
              HorizontalAlignment="Center"
              MaxWidth="900"
              RowSpacing="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Spacing="4">
                <TextBlock Text="TodoApp" Style="{StaticResource TitleTextBlockStyle}" />
                <TextBlock Text="WinUI 3 presentation layer / Application / Domain / Infrastructure"
                           Opacity="0.7" />
            </StackPanel>

            <Grid Grid.Row="1" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox Header="タイトル" PlaceholderText="タスクのタイトル"
                         Text="{x:Bind ViewModel.NewTitle, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Grid.Column="1" Header="説明" PlaceholderText="任意"
                         Text="{x:Bind ViewModel.NewDescription, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Column="2" Content="追加" VerticalAlignment="Bottom"
                        Command="{x:Bind ViewModel.AddCommand}" />
            </Grid>

            <StackPanel Grid.Row="2" Spacing="12">
                <InfoBar IsOpen="{x:Bind ViewModel.HasError, Mode=OneWay}"
                         Severity="Error" Message="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" />
                <ListView ItemsSource="{x:Bind ViewModel.Todos, Mode=OneWay}"
                          HorizontalContentAlignment="Stretch"
                          SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="todos:TodoDto">
                            <Grid Padding="8" ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="{x:Bind Title}" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                    <TextBlock Text="{x:Bind Description}" Opacity="0.7" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" Orientation="Horizontal"
                                            VerticalAlignment="Center" Spacing="8">
                                    <TextBlock Text="{x:Bind IsCompleted}"
                                               VerticalAlignment="Center" />
                                    <Button Content="完了"
                                            Command="{Binding ElementName=Page, Path=DataContext.ViewModel.CompleteCommand}"
                                            CommandParameter="{x:Bind}" />
                                    <Button Content="削除"
                                            Command="{Binding ElementName=Page, Path=DataContext.ViewModel.DeleteCommand}"
                                            CommandParameter="{x:Bind}" />
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
